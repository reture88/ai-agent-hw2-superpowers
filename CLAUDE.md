# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

This repository (`ai-agent-hw2-superpowers`) is currently empty — it contains only a `README.md`. There is no source code, build system, test suite, or dependency manifest yet.

Once real code, a package manifest, and tooling exist, update this file with:
- Build / lint / test commands (including how to run a single test)
- High-level architecture and module boundaries
